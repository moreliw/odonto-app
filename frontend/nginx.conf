server {
  listen 8080;
  server_name _;
  root /usr/share/nginx/html;
  client_max_body_size 25m;
  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "DENY" always;
  add_header Referrer-Policy "strict-origin-when-cross-origin" always;
  add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

  # Sem hash no nome dos bundles (main.js, styles.css...), navegadores aplicam
  # cache heurístico e ficam presos em JS antigo depois de um deploy — foi
  # exatamente a causa de chamadas a rotas já removidas aparecerem no console
  # após a atualização do dashboard. Força revalidação (via ETag) em vez de
  # cache heurístico, sem desabilitar cache por completo.
  location ~* \.(js|css)$ {
    add_header Cache-Control "no-cache";
    try_files $uri =404;
  }
  location = /index.html {
    add_header Cache-Control "no-cache";
  }

  # Link curto usado nas mensagens. O backend entrega metadados Open Graph da
  # clínica e redireciona o paciente para a tela Angular de confirmação.
  location /c/ {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_pass http://app-backend:3000/api/public/appointments/share/;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_read_timeout 120s;
    proxy_pass http://app-backend:3000/api;
  }

  # Uploads assinados permanecem no mesmo domínio HTTPS do aplicativo. Isso
  # evita mixed-content no navegador e mantém o MinIO isolado na rede Docker.
  location ~ ^/(odonto-files|odonto-files-dev)/ {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_request_buffering off;
    proxy_read_timeout 120s;
    proxy_pass http://minio:9000;
  }
}
